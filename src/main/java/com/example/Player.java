package com.example;

public final class Player {
    private Worker[];

    public Player(Worker[] workers) {
        this.Worker[] = workers;
    }

    public Worker[] getWorkers { return this.Worker[]; }

}